on: [push]

jobs:
  security:
    uses: ckalpakoglu/decsecops-workflows/.github/workflows/security.yaml@main
    with:
      host: https://appsecsanta.invicti.com
    secrets:
      KONDUKTO_TOKEN: ${{ secrets.KONDUKTO_TOKEN }}
